This directory indicates the route of the react application. Each file in this flder contains its route. 

A page can contain its subfolder. Each page has its state and is usually used to call an async operation. 
