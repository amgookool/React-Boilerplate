This directory contains a collection of UI components like buttons, modals, inputs, loader, etc., that can be used accross various files in the project. 

Each component should consists of a test file to do a unit test as it will be widely used in the project.  
